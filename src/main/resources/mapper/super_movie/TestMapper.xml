<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.super_movie.mapper.TestMapper">
    <select id="getMagic" resultType="String" parameterType="java.lang.Integer" >
        select  magic_id
        from test
        where id = #{id}
    </select>
</mapper>
